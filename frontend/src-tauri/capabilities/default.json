{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability of the main window: core only, no plugin.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
