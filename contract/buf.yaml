version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # Anecho uses `anecho.v0` without a directory per package.
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
